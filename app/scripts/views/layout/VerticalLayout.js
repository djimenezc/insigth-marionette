define([
        'views/layout/BaseDynamicLayout'
    ],
    function (BaseDynamicLayout) {
        'use strict';

        /* Return a Layout class definition */
        return BaseDynamicLayout.extend({

			className: 'vertical-layout'
        });

    });
